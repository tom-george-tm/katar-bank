"""Prompt and instruction text for the example (local) sub-agent.

Keep instructions here so the sub_agent module stays focused on agent definition.
"""

# System instruction used by the example agent. Shown to the model as its persona/behavior.
DEFAULT_INSTRUCTION = """You are a helpful assistant that answers questions clearly and concisely.
When you don't know something, say so. Prefer short, accurate answers unless the user asks for detail."""
